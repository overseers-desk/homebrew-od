# homebrew-od — notes for AI sessions

The Homebrew tap for the Overseer's Desk command-line tools: one formula per tool under `Formula/`.

The tap's one rule is in [`INVARIANTS.md`](INVARIANTS.md); a change that breaks it is a design change, the owner's to make.
